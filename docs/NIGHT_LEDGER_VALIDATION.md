# Night Ledger Validation

The persistent mode control is visible in the public desktop header and mobile header. It displays the active mode, uses an icon plus label, exposes a pressed state for assistive technology, and preserves the existing browser-storage preference behavior.

At desktop width, the Night Ledger palette keeps the hero headline, navigation, call-to-action controls, trust line, and image-overlay cards readable. The light overlay cards explicitly retain dark text in Night Ledger rather than inheriting the global light-on-dark foreground.

At a 390 px phone viewport, the mode control remains visible above the compact notification control and menu button. The hero hierarchy, actions, trust line, and image preview remain legible without horizontal overflow.
