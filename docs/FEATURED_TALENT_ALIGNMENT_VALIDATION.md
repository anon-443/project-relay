# Featured Talent Alignment Validation

The homepage was reviewed at **1919 × 1000** and **390 × 844** after the featured-talent layout adjustment. On desktop, the section introduction uses the full row with the editorial image aligned at its right, and the directory begins below at the left edge of the content shell. The profile cards no longer begin in a detached right-side column or leave a split-layout gap.

The footer statement remains on one line at desktop width. On phone-sized screens, the talent introduction returns to a single-column flow, the directory stays full width, and the footer statement can wrap rather than overflow.
